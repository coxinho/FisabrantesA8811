@model IEnumerable<Fisabrantes.Models.Prescricoes>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Home/About.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Consulta.idConsulta)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descricao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Consulta.idConsulta)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descricao)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idPrescricao }) |
            @Html.ActionLink("Details", "Details", new { id=item.idPrescricao }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idPrescricao })
        </td>
    </tr>
}

</table>
